# Test layout

This directory is intentionally created in phase 1.

- `tests/unit/` for fast unit tests.
- `tests/integration/` for cloud and file integration flows.
- `tests/e2e/` for UI and workflow smoke tests.
- `tests/goldens/` for PWMB decode references.

